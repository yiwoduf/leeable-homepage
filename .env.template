# Add real secrets here (API keys, tokens, etc.)
# Site content (name, socials, experience) lives in src/config/siteData.ts — not env vars
# VITE_* prefix required for Vite to expose vars to the client bundle
